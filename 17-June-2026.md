17-June-2026\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_View:- View is a logical table which is created from another table.If you perform insert, update or delete in view then main table will also be affected.create table staff(staffid int primary key,staffname varchar(50)department varchar(50),salary bigint);**Syntax:-****---------****create view <view\_name> as select <column1., <column2>, from <table\_name>;**E.g.create view stf as select staffid, staffname, department from staff;insert into stf values(1001, 'Ajay', 'Management');insert into stf values(1002, 'Tufail','Develoment');Now we drop the view named 'stfdrop view stf;drop dtabase fdb3;create database fdb3;Stored Procedure:- Stored Procedure is a pre-compiled is a pre-compiled query, which is developed in database in form of procedure and it is consumed in backend.18- June- 2026\---------------select \* from staff where staffid=1001delete from staff where staffid=100Table Name : customercid         int           primary Key         A\_Icname       varchar(50)                          Not NULLaddress     varchar(500)contactno    varchar(15)Group By:- Same values ka group bna deta hai.Group by is used to arrange rows having same values into groups. It is commonly used with aggregate function.create table spiemp(empid int primary key,empname varchar(50),department varchar(50),salary bigint);empid     |      empname       |        department        |             salary1         |      Amit          |        HR                |           300002         |      Ravi          |        IT                |           500003         |      Priya         |        HR                |           350004         |      Neha          |        IT                |           450005         |      Mohan         |        Sales             |           400006         |      Karan         |        IT                |           55000insert into spiemp values(1, 'Amit', 'HR', 30000);insert into spiemp values(2, 'Ravi', 'IT', 50000);insert into spiemp values(3, 'Priya', 'HR', 35000);insert into spiemp values(4, 'Neha', 'IT', 45000);insert into spiemp values(5, 'Mohan', 'Sales', 40000);insert into spiemp values(6, 'Karan', 'IT', 55000);Example - 1:- Count Employees in each department.&#x20;select department, count(\*)"No. of Employees" from spiemp group by department;Example - 2:- Find Total Salary Department-wise.select department, sum(salery)"Total salary" from spiemp group by department;